---
import type { IconProps } from "../types";

interface Props extends IconProps {}
const { size, color, className } = Astro.props;
---
<svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" class={className}>
	<path fill={color || "currentColor"} d="M3 18v-2h18v2zm0-5v-2h18v2zm0-5V6h18v2z" />
</svg>